#!/usr/bin/env python3
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from _hooklib import *  # noqa: F401,F403
"""certificate-rotation: rotate certificates with overlap, reconnect workers, retire the old credential.
Command: CHROMOFOLD_HOOK_ROTATE_CERT_CMD."""
t = Timer(); p = read_payload(); ep = guard_runtime(t, p)
control_scenario(t, p, ep, "ROTATE_CERT")
